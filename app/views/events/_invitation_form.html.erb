<%= form_with model: Invite.new do |form| %>
  <%= form.hidden_field :event_id, :value => @event.id %>
  <%= form.hidden_field :user_id, :value => current_user.id %>
  <%= form.submit "Join Event" %>
<% end %>